name: Retired changelog migration

on:
  push:
    branches:
      - retired-changelog-migration

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Migration status
        run: echo "Changelog migration completed; no repository changes are performed."
