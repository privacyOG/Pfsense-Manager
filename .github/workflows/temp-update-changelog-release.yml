name: Retired changelog migration

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Migration status
        run: echo "The changelog migration has already been completed. No repository changes are performed."
