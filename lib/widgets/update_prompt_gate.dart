import 'package:flutter/material.dart';

class UpdatePromptGate extends StatelessWidget {
  const UpdatePromptGate({super.key, required this.child});
  final Widget child;

  @override
  Widget build(BuildContext context) => child;
}
